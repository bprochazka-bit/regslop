# Minimized crash and divergence reproducers found by the fuzzers.
# Each <kind>_<sig>.yaml (op/data) or hivecrash_*.hiv (+ .txt manifest) replays a
# single filed finding. Committed so they double as regression cases.
