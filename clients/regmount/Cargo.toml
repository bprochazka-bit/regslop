[package]
name = "regmount"
version = "0.1.0"
edition = "2021"
description = "Generate a libreg mount map by inspecting hive files"
license = "MIT OR Apache-2.0"

[[bin]]
name = "regmount"
path = "src/main.rs"

[dependencies]
cli-core = { path = "../cli-core" }
