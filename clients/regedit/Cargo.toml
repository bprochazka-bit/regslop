[package]
name = "regedit"
version = "0.1.0"
edition = "2021"
description = "Web-based regedit for offline hives, built on libreg"
license = "MIT OR Apache-2.0"

[[bin]]
name = "regedit"
path = "src/main.rs"

[dependencies]
cli-core = { path = "../cli-core" }
