[package]
name = "sc"
version = "0.1.0"
edition = "2021"
description = "sc.exe-compatible offline service configuration tool"
license = "MIT OR Apache-2.0"

[[bin]]
name = "sc"
path = "src/main.rs"

[dependencies]
cli-core = { path = "../cli-core" }
